project_name: grpc-header-mapper

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - skip: true

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - '^ci:'
